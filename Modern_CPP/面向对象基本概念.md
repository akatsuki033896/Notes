# 重载 / 重写 / 隐藏

## 重载 *Overload*

在相同作用域(命名空间 / 同一个类)内拥有相同的函数名，但具有不同的参数类型和/或参数数量，重载允许根据所提供的参数不同来调用不同的函数。

- 函数具有相同的名称。
- 函数具有不同的参数类型或参数数量。
- 返回类型可以相同或不同。
- 同一作用域，比如都是一个类的成员函数，或者都是全局函数

## 重写 *Override*

在派生类中重新定义基类，当派生类需要改变或扩展基类方法的功能时，就需要用到重写。



